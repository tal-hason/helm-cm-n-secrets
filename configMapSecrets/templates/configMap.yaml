{{- range .Values.configMaps }}
{{- if .enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .name }}
  labels:
    helm-chart/release-name: {{ $.Release.Name }}
    helm-chart/release-version: {{ $.Release.version }}
    {{- range $key, $value := $.Values.commonLables }}
    {{ $key }}: {{ $value | quote }}
    {{- end }} 
  annotations:
    {{- range $key, $value := $.Values.commonAnnotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }} 
data:
  {{- if eq .type "string" }}
  {{ .fileName }}: |
    {{- tpl .data $ | nindent 4 }}
  {{- else if eq .type "keyValues" }}
  {{- range $key, $value := .data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  {{- end }}
{{- end }}
{{- end }}
